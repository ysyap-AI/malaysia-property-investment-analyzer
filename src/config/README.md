# Configuration (not implemented)

Reserved for Phase 1 step 7.

Planned files:
- `finance-defaults.ts` — Malaysian stamp duty / legal fee / vacancy defaults,
  each labelled `Estimated` with a source note.
- `scoring-config.ts` — metric bands, weights, risk thresholds, scenario
  adjustments, recommendation rule table.

Every config object carries a `version`; analysis snapshots store the version used.
